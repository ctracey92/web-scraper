<div class="container">
    <div class="row">
    <div class="col-8">
    <div class="main-content">
        <div class="jumbotron jumbotron-fluid">
            <div class="container">
                <h1 class="display-4">Web Scraper</h1>
                <p class="lead">Click to scrape!</p>
                <button type="button" class="btn btn-primary scrape">Scrape!</button>
            </div>
        </div>
        <div class="container overflow-auto articles">
            {{#each articles}}
            <h4>{{title}}</h4>
            <p>{{summary}}</p>
            <a href="{{link}}" target="_blank" class="btn btn-primary">Link to article</a>
            <a href="/articles/{{_id}}" class="btn btn-danger">Comment Here!</a>
            <p>---------------</p>
            {{/each}}
        </div>
    
    </div>  
    </div>
    {{!-- <div class="col-4">
        <div class="comments-section">
            <h5 id="grabbedArticle" data-id="{{_id}}">Article:{{title}}</h5>
            <div class="comments overflow-auto">

            </div>
            <form>
                <div class="form-group">
                    <label for="commentBox"></label>
                    <textarea class="form-control" id="commentBox" rows="2"></textarea>
                    <button type="submit" class="btn btn-primary" id="submitComment">Submit</button>
                </div>
            </form>

        </div>  
    </div> --}}
</div>

</div>

